import React from "react";

function Card(props) {
    return(
        <div className="">
            <div>

            </div>
            <div>{props.children}</div>
        </div>
    )
}

export default Card;